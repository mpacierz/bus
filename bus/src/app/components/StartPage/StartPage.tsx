import React from 'react';
import { Link } from 'react-router-dom';

const StartPage: React.FC = () => {
    return (
        <div className='bus-app-page'>
            <Link to='/'>HP5 test</Link>
        </div>
    );
}

export default StartPage;